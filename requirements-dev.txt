# Development and testing dependencies
# (includes everything needed to run the test suite; audible-cli itself is
# only needed at runtime for downloading, not for tests)
Pillow>=10.0.0
requests>=2.31.0
pytest>=7.0.0
